{
  "name": "word_counter",
  "description": "Counts words in text",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Text to count"
      }
    },
    "required": ["text"],
    "additionalProperties": false
  }
}